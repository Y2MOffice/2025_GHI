import React from "react";
import { Box, Typography } from "@mui/material";

const TradeLaw = () => {
  return (
    <Box
      sx={{
        minHeight: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Typography variant="h4">Trade Law</Typography>
    </Box>
  );
};

export default TradeLaw;
